namespace DestinationDiscoveryService.DTOs
{
    public class LanguageDTO
    {
        public string Name { get; set; }
        public string Code { get; set; }
        // Possibly exclude the CountryFlag if not needed
    }

}